# mobile — 移动端 APP

**目录预留,当前不开发**(决策见 docs/decisions/004-web-first-mobile-later.md)。

## 规划

- 启动时机:PC 网站(apps/web)交付、`packages/api-spec` 契约冻结之后;
- 选型倾向 React Native(与 web 共享 TypeScript 类型与业务逻辑),最终决策推迟到启动时;
- 接入前提:后端能力全部经 packages/api-spec 契约暴露,移动端接入预期零后端改动。

## 当前状态

仅保留目录与本说明,不放置任何代码。
